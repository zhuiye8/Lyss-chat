/**
 * 模型分发管理页面 (管理员)
 * 模型权限分配、配额管理
 */

import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const Models: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Card>
        <Title level={2}>模型分发</Title>
        <p>模型分发管理界面开发中...</p>
      </Card>
    </div>
  );
};

export default Models;